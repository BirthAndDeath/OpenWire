# AGENTS.md — OpenWire 代码规范 / Code Conventions

## 核心原则：精简即安全 / Simplicity Is Security

**每个多余的字段、参数、分支、抽象都是潜在的失配点、漂移点和攻击面。**
代码越简单，越少出错，越少被绕过。审查与实现都应把「能否更少」作为第一问题。

平台安全优先级：**身份与密钥安全 > 网络输入校验 > 通道背压 > 性能**。
设计任何改动前先问：这个输入来自网络吗？这个值能写出磁盘吗？这条路径可能 panic 吗？

---

## 1. 命名规范 / Naming

- **名称即文档**：标识符必须精确描述职责与语义；禁止无意义缩写（`q`、`arr`、`tmp`、`data2`）、动词性布尔（`is_ok` 之外再套 `is_success`）、含义模糊的泛称（`handler`、`manager`）。
- **与协议/存储字段对齐**：库表列名、SQL 绑定序、wire 格式字段名必须一一对应，禁止同名异义或异名同义。schema 变更时三者同步修改。
- **类型即语义**：用类型表达不变量——`Option` 表达“可能缺失”，`Result` 表达“可能失败”，`Zeroizing<T>` 表达“敏感数据”，`&[u8]` 与 `&str` 不混用。
- **布尔/枚举二选一**：状态多于两个值就用枚举（如 `TransferStatus`），三态布尔与哨兵值（`""`、`-1`、`255`）仅在与外部格式对齐时使用，并注明原因。

## 2. 结构与简洁性 / Structure & Simplicity

- **最小改动面**：修 bug / 加功能时只改必要文件与必要行；禁止顺手重构无关代码、清理无关 warning、添加“顺带”的抽象。
- **不为不存在的需求建抽象**：不添加无调用方的函数、结构体、字段、参数、trait 方法、feature gate。单个值 1~2 处使用用普通参数；参数穿透超过 3 层是设计信号而非解法——优先消除调用链。
- **一个概念只保留一份表示**：不维护两套语义重叠的字段/哈希/键（去重键与签名目标用同一个值）。能用协议已有字段（如 `ChatMessage.hash`）就不再造本地等价物。新列/新参数必须先论证「现有字段无法表达」。
- **零死代码**：重命名或重构后旧名称必须彻底删除并 grep 验证零引用（含 re-export）。`#[allow(...)]`/`#[cfg]` 屏蔽的警告与分支必须核实语义，宁可删除不可压住。
- **Feature 即承诺**：default features 内的代码不得存在 `#[cfg(not(feature = ...))]` 死分支；feature 若从未启用，定义与门控一并删除。
- **数据库轻量化**：能被哈希/协议字段覆盖的数据（nonce、timestamp）不单独建列。开发版本无向后兼容约束：结构变更直接改迁移，不保留兼容层与自动迁移代码。

## 3. 安全最佳实践 / Security

- **不信任网络输入**：所有来自网络的数据（消息、地址、文件名、公钥 hex）进入逻辑前必须校验：长度、格式、枚举值、范围。非法输入返回错误并告警日志，绝不到达 `unwrap`/切片索引/`parse`。
- **禁止不可达 panic**：对外部输入的所有路径禁用 `unwrap()`/`expect()`/裸切片 `[..n]`/`parse()` 不校验。仅在「内部不变量，由本 crate 保证」时允许，并留注释指明不变量来源。
- **路径与标识安全**：文件名/路径必须防穿越（拒绝 `..`、绝对路径、分隔符、控制字符）。写入磁盘前经规范化校验。对称密钥派生（HKDF info 参数等）一旦定型不得修改，否则旧数据全部失效。
- **密钥与敏感数据**：私钥、口令、master key 全程 `Zeroizing`，内存驻留句柄 drop 前清零并考虑 mlock；序列化缓冲同样零化。密钥只存系统 keyring + AES-GCM 加密文件。禁止把密钥写入日志、错误消息或数据库明文列。
- **鉴别用常数时间比较**：签名、MAC、口令、哈希对比用 `subtle`/`constant_time_compare`，禁止 `==` 短路比较。
- **通道与背压**：`try_send` 失败必须处理——丢弃可接受时显式说明，否则记录日志或走重试/降级。不要静默吞错导致“看似已提交实际未发送”。
- **资源上限**：所有可被对端填充的集合（路由表、缓存、listen 地址、速率 map）必须有上限；缓存满时淘汰而非无限增长。
- **开发版本无兼容回退**：不保留旧格式解密回退、旧协议分支等迁移层，直接升级并删旧（见「一个概念只保留一份表示」）。

## 4. 注释规范 / Comments

- **解释 why，不重复 what**：复杂/易混淆逻辑（并发时序、密码学构造、不可达分支）注释说明原因与不变量；显而易见的内容不加注释，禁止「给变量赋值」式注释。
- **简洁正确**：注释要短，更新代码必须同步更新注释。可推导内容（类型、调用链）不值得注释。
- **不可达分支必须留痕**：理论不可达的分支须说明为何不可达；可被外部触发转而不可达时，保留日志路径而非静默。

## 5. 提交前自检 / Pre-commit Checklist

- `cargo check --workspace` 通过且不引入新 warning；`cargo clippy --workspace` 不新增告警。
- grep 确认删除/重命名的符号零残留（含 re-export、feature 名）。
- 安全检查：网络输入是否全部校验？新增 panic 路径？密钥入日志？路径可穿越？
- 问自己：这 3 个字段/参数/函数中，哪一个是我真的需要的？

## 6. 审查清单 / Review Checklist

- 改动能否再砍一半？砍掉的部分会不会丢功能？
- 命名是否语义明确、与协议/存储字段一致？是否用了哨兵值替代枚举？
- 是否存在两个来源描述同一件事（哈希、时间、状态、类型）？
- 是否有死代码、过期注释、压住的 warning 或从未启用的 feature？
- 网络输入的所有路径是否无 panic、有上限校验？敏感数据是否零化且不入日志？
- 参数、返回元组、中间结构是否超过必要数量？