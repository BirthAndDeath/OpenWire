# AGENTS.md — OpenWire 代码规范

## 核心原则：精简即安全（Simplicity Is Security）

**每个多余的字段、参数、分支、抽象都是潜在的失配点、漂移点和攻击面。**
代码越简单，越少出错，越少被绕过。审查和实现都应把「能否更少」作为第一问题。

### 强制规则

1. **最小改动面**
   - 修 bug / 加功能时，只改必要文件与必要行。一个需求对应一组最少的改动。
   - 禁止顺手重构无关代码、清理无关警告、添加"顺带"的抽象。

2. **不为不存在的需求建抽象**
   - 不添加无调用方的函数、结构体、字段、参数、trait 方法（如 `new_xxx_with` 变体）。
   - 单个值只在 1~2 处使用就用普通参数；3+ 处才开始考虑聚合结构。
   - 参数穿透超过 3 层，是设计信号而非解法：优先消除调用链，而不是加更多参数。

3. **一个概念只保留一份表示**
   - 不要维护两套语义重叠的字段/哈希/键（例如去重键与签名目标必须用同一个值）。
   - 能用协议已有字段（如 `ChatMessage.hash`）就不再造本地等价物。
   - 新列/新参数必须先论证"现有字段无法表达"，否则就是冗余。

4. **零死代码**
   - 函数重命名或重构后，旧名称必须彻底删除并 grep 验证零引用（含 re-export）。
   - 分支若理论不可达，必须留注释说明原因，而不是留误导性文案。

5. **数据库轻量化**
   - 能被哈希/协议字段覆盖的数据（nonce、timestamp 等）不再单独建列。
   - 开发版本无向后兼容约束：结构变更直接改迁移，不保留兼容层。

6. **提交前自检**
   - `cargo check --workspace` 通过，且不引入新的 warning。
   - grep 确认删除的符号无残留调用。
   - 问自己：这 3 个字段/参数/函数中，哪一个是我真的需要的？

### 审查时的简洁性检查清单

- 改动能否再砍一半？砍掉的部分会不会丢功能？
- 是否存在两个来源的数据描述同一件事（哈希、时间、状态）？
- 是否有死代码/旧名称残留？
- 参数、返回元组、中间结构是否超过必要数量？
